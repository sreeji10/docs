---
title: "(PROVIDER_NAME) middleware integration"
description: "Integrate with the (PROVIDER_NAME) middleware using LangChain Python."
---
---

How to use this Python middleware template:

- [ ] Substitute (PROVIDER_NAME) with the name of the provider, e.g. Anthropic, OpenAI, etc. (`command + F` is your friend here)
- [ ] Substitute (MIDDLEWARE_CLASS) with the name of the middleware class, e.g. `OpenAIModerationMiddleware`
- [ ] Update links to point to the correct module
- [ ] Update the PyPI/registry package name if needed
- [ ] Update the API key environment variable name if needed
- [ ] Fill in the middleware table with each middleware your package provides
- [ ] For each middleware, add a section with API reference, code example, configuration options, and a full example

The template starts below this line...

Middleware specifically designed for (PROVIDER_NAME) models. Learn more about [middleware](/oss/langchain/middleware/overview).

| Middleware | Description |
|------------|-------------|
| [Middleware name](#middleware-name) | Brief description of what it does |

## Setup

### Credentials

```python Set API key icon="key"
import getpass
import os

if "(PROVIDER_NAME)_API_KEY" not in os.environ:
    os.environ["(PROVIDER_NAME)_API_KEY"] = getpass.getpass("Enter your (PROVIDER_NAME) API key: ")
```

### Installation

<CodeGroup>
    ```python pip
    pip install -U langchain-(PROVIDER_NAME)
    ```
    ```python uv
    uv add langchain-(PROVIDER_NAME)
    ```
</CodeGroup>

## Middleware name

Description of what this middleware does and when to use it.

**API reference:** @[`(MIDDLEWARE_CLASS)`]

```python
from langchain_(PROVIDER_NAME) import Chat(PROVIDER_NAME)
from langchain_(PROVIDER_NAME).middleware import (MIDDLEWARE_CLASS)
from langchain.agents import create_agent

agent = create_agent(
    model=Chat(PROVIDER_NAME)(model="model-name"),
    tools=[...],
    middleware=[
        (MIDDLEWARE_CLASS)(
            # Configuration options
        ),
    ],
)
```

<Accordion title="Configuration options">

<ParamField body="param_name" type="type" default="default_value">
    Description of the parameter
</ParamField>

</Accordion>

<Accordion title="Full example">

```python
from langchain_(PROVIDER_NAME) import Chat(PROVIDER_NAME)
from langchain_(PROVIDER_NAME).middleware import (MIDDLEWARE_CLASS)
from langchain.agents import create_agent
from langchain.messages import HumanMessage

agent = create_agent(
    model=Chat(PROVIDER_NAME)(model="model-name"),
    tools=[],
    middleware=[
        (MIDDLEWARE_CLASS)(
            # Configuration options
        ),
    ],
)

result = agent.invoke({"messages": [HumanMessage("Hello")]})
print(result["messages"][-1].content)
```

</Accordion>
