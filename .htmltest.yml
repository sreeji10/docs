# htmltest configuration for docs link check
# See https://github.com/wjdp/htmltest for options

# Reduce concurrency to avoid 429 rate limits from external servers
HTTPConcurrencyLimit: 4
DocumentConcurrencyLimit: 8

IgnoreURLs:
  - "mintcdn.com"
  - "mintlify-assets"
